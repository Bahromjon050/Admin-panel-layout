import React from "react";
import Sidebar from "../layout/Sidebar";
import Registration from "../components/Registration";

const Registerations = () => {
  return <Sidebar data={<Registration />} />;
};

export default Registerations;
